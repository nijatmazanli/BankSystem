package com.banking.banksystem.repository;

import com.banking.banksystem.model.TokenAdd;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TokenAddRepository extends JpaRepository<TokenAdd, Long> {
}
